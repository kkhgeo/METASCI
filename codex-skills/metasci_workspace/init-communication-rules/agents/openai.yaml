interface:
  display_name: "Init Communication Rules"
  short_description: "Append a 'Communication Rules' block (AI-tell removal for agent↔author dialogue, adapted from pstack's unslop) to an existing CLAUDE.md and AGENTS.md in a manus"
  default_prompt: "Use $init-communication-rules for this task."
