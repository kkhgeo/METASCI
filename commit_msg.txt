Replace rendering rules with content contract; batch paragraph review

- output_format.md v13: delete the typography layer entirely (boxes,
  hand-drawn rules, width math). Visual layout is now at the model's
  discretion within standard Markdown. Keep a short mandatory content
  contract: Korean + English originals, Top-3 tier discipline, unified
  severity (높음/중간/낮음/치명) and reference status (확인됨/추정/미확인)
  labels, plain-language explanations, next-actions line per screen
- Mode 3 redesign: ONE reviewer panel round per paragraph covering both
  paragraph-level and per-sentence findings (was one round per sentence;
  ~10x wall-clock reduction). Walkthrough steps only through flagged
  sentences; clean sentences summarized in one line
- Batch commands: 전부 적용 / 합의만 적용 / 나머지 건너뛰기
- Decision points now use the AskUserQuestion tool (buttons) with typed
  commands still honored: distribution approval, intent confirmation,
  sentence decisions, conflict resolution
- Fast path: Case D (no knowledge files / pasted text) skips the
  distribution approval ceremony
- Purge v12 line-pattern and card-format references from deliberation,
  confidence_routing, agent_b; merge Mode 3 context loading into entry

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
