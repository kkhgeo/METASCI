# 출처와 동기화 규칙

이 폴더는 `claude_writing_manual/`(별도 관리되는 글쓰기 매뉴얼, 2026-08-09 빌드)에서
단락 리뷰에 쓰이는 9개 파일만 복사한 **사본**이다 (복사일: 2026-08-18).

- **원본(상류)**: `Z:\KKH_Research\META_SCI\claude_writing_manual\` — 327개 원리 전체,
  INDEX.md, 검증 기록, `_remined/` 포함. 수정은 항상 원본에서 하고 이 폴더로 재복사한다.
  이 사본을 직접 고치면 다음 재복사에서 소실된다.
- **제외한 파일**: `08_authorship`(저자 표기), `09_submission`(투고), `10_thesis_to_article`,
  `11_writing_process`, `12_ai_use`, `INDEX.md`, `DIAGNOSTIC-MAP.md`, `_remined/` —
  단락 수준 리뷰 범위 밖이거나 빌드 메타데이터.
- **사용 규칙 요약** (원본 INDEX.md에서): 수렴 수는 조언 문헌이 얼마나 자주 반복했는가이지
  규범성이 아니다 — 수렴 수로 순위 매기지 말 것. `[~] Verified with drift` 항목은 인용 전
  원 PDF에서 재인용할 것. `[!] Convergence inflated`는 트위터 계정 분할 중복 표시.
- **`../section-checklists.md`와의 관계**: 같은 코퍼스의 두 용도다 — 체크리스트는 진단용
  (이진 판정 + 심각도), 이 폴더는 기준 진술용(원리 + verbatim 인용 + ID). 내용 충돌 시
  `principles.md` > 체크리스트 > 매뉴얼 순으로 우선한다 (2026-08-10 대조 감사: 모순 0).
  이 폴더를 재복사할 때는 체크리스트와 어긋난 항목이 새로 생기지 않았는지 함께 확인할 것.
