# CHANGELOG — meta-rewriting

이전에는 버전 표기가 없고 참조 파일의 날짜 헤더(2026-07-11, 2026-07-16)만 있었다.
`meta-writing`과 표기를 맞추기 위해 이번에 버전 체계를 도입한다.
직전 GitHub 상태(단락 리뷰 버전)를 **1.0.0**으로 소급 지정한다.

## 2.3.0 (2026-08-18)

사용자 결정: "경로를 주는 것보다 스킬 폴더에 레퍼런스로 아예 넣어주는 게 좋지 않나."

매뉴얼을 외부 경로 참조에서 **동봉 사본**으로 전환. `claude_writing_manual/`에서 단락
리뷰에 쓰는 9개 파일(00_universal, 01~07 섹션, DECISIONS)만 `references/manual/`로
복사했다 (148KB). 제외: 08~12권(저자 표기·투고·학위논문 전환·집필 과정·AI 사용 —
단락 리뷰 범위 밖), INDEX·DIAGNOSTIC-MAP·`_remined/`(빌드 메타데이터).

- 절대경로(`Z:\...`)와 폴백 분기 제거 — 스킬이 다시 완전 자체 완결이 되어 GitHub
  배포 시 어느 환경에서든 동일 동작.
- `references/manual/_SOURCE.md` 신설 — 원본 위치, 복사일, 제외 목록, 동기화 방향
  (수정은 원본에서, 사본은 재복사) 기록. 매뉴얼 사용 규칙 3개(수렴 수 ≠ 규범성,
  drift 재인용, inflated 표시)도 여기로 요약 이관.
- Stage 2가 `references/manual/`을 직접 지시. DECISIONS.md 동봉으로 venue-bound
  질문 3건의 양측 입장도 스킬 안에서 열람 가능.

## 2.2.1 (2026-08-18)

iteration-2 검증에서 발견된 결함 패치. 원칙 우선 압력 하에서 날조가 한 형태로
재발했다: 한국어 초고의 "저어콘 연대측정도 **함께** 수행하였는데"를 영역하며
A안 "on the same samples" / B안 "on a subset of the samples" — 원문에 없는 시료
관계를 서로 모순되게 구체화했다 (기존 가드레일은 기기명·수치만 명시적으로 막았음).

④에 조항 신설: **"모호함도 사실이다 — 번역하되 해소하지 말 것."** 모호한 관계
표현(함께, 등)은 같은 모호도로 옮기고("in parallel"), 특정 해석으로 확정하는 것은
기기명 날조와 동일한 날조로 규정. 모호함 자체가 결함이면 ②에서 지적하고
`[근거 필요]`로 표시한다.

## 2.2.0 (2026-08-18)

사용자 피드백: "지금 수정이 기존 글에 너무 의존한다. 학술적 글쓰기의 원칙을 잘
따랐으면 좋겠다. claude_writing_manual을 바로 스킬로 줘야 하는 것 아닌가."

리라이팅의 무게중심을 **원문 보존 → 원칙 구현**으로 옮겼다. 사실 보존(수치·인용
verbatim, 날조 금지)은 그대로지만, *문장·표현*의 보존은 더 이상 미덕이 아니다.

- **Stage 2에 writing manual 필수 로드 신설.** `claude_writing_manual/`의
  `00_universal.md` + 해당 섹션 파일 1개를 진단 전에 읽는다. 경로는
  `Z:\KKH_Research\META_SCI\claude_writing_manual\` → 스킬 상대경로
  `../meta-writing/references/claude_writing_manual/` 순으로 탐색, 둘 다 없으면
  references/만으로 동작(무손실 폴백 — 체크리스트가 같은 코퍼스의 압축본).
  사용 규칙 3개를 함께 이식: 수렴 수 ≠ 규범성, `[~] drift` 인용 전 재확인,
  중복 시 principles.md 우선.
- **C안 재정의 — 원칙 모범판.** "로드한 원칙이 쓸 단락을 쓴다": 원문 문장이
  없다고 치고 주제문 우선·old-before-new·한 문장 한 아이디어·간결·능동태·평이어·
  중간 헤징·C-C-C를 전면 적용해 새로 쓴다. 닻은 저자의 사실과 의도이지 원문
  표현이 아니다.
- **⑤ 판정 기준 명시 — 원칙 준수로 판정, 원문 근접성은 장점이 아니다.** 저자
  표현에 대한 공손함으로 A/B를 권하는 것은 리뷰의 목적을 배반한다. C가 학술
  산문으로서 더 나으면 C를 권한다. (과잉교정 브레이크는 *반박된 규칙*을 막는
  것이지 원칙에 따른 재구성을 막는 것이 아님을 병기.)
- **② 진단에 manual 원리 ID 인용** (예: `paragraph-logic-21`) — 진단이 개인
  판단이 아니라 표준으로 역추적되게. 적용 지침 명시 줄에 manual 파일 포함.
- "원문이 최선일 수 있다" 조항 보정: 원문 승리는 *원칙 준수의 판정 결과*여야
  하며, 원문이라는 이유로 선호하는 면허가 아니다.

2.0.0의 "자체 완결" 원칙과의 관계: manual은 필수 로드지만 **부재 시 무손실
폴백**이 있으므로 스킬 단독 배포는 여전히 깨지지 않는다.

## 2.1.0 (2026-08-18)

사용자 요청: "이 스킬이 결과를 반영할 때 국문(번역본)을 같이 줘야 돼."

- **④ 각 대안에 국문 번역본 동반.** 영문 대안 단락마다 그 아래 국문 번역을 붙인다.
  번역은 저자가 의미를 한눈에 검증하기 위한 **이해 보조물**이며 원고용 한국어 산문이
  아니다 — 어떤 한국어 레지스터 기준도 통과하지 않았음을 언어 규칙에 명시했다.
  번역 대상은 원문이 아니라 **그 대안이 실제로 말하는 내용**이다(`[근거 필요]` 마커 포함).
- Follow-up의 「한국어 출력 요청 → 거절」 항목을 「한국어 *원고* 요청 → 거절」로 좁혔다.
  번역은 이제 기본 제공되므로, 거절 대상은 한국어로 *남을* 원고 산문뿐이다
  (`meta-mywriting-korean` 안내 유지). 채택·부분 수정 시에도 번역을 함께 낸다.
- Completion criterion에 번역 동반을 추가.

## 2.0.0 (2026-08-18)

전면 재설계. GitHub 정본 v1.1.0을 베이스로, 사용자가 정의한 4단계 흐름 —
**(1) 초고 단락 입력 → (2) 글쓰기 원리·지침 로드 → (3) 문제 진단 → (4) 대안 단락 제공** —
을 문서의 뼈대로 삼아 SKILL.md를 다시 썼다. skill-creator로 재작성·비교 테스트 수행.

### SKILL.md — 4단계 재구성과 v1.1.0 결함 수정

- **4단계 프로세스 명시.** 기존 문서는 출력 5블록만 규정하고 작업 순서는 암묵적이었다.
  Stage 1 입력 / Stage 2 원리 로드 / Stage 3 진단(①②③) / Stage 4 대안(④⑤)으로 분리하고,
  "원리를 로드하기 전에 진단하지 말 것"을 구조로 강제했다.
- **섹션 미상 시 침묵 스킵 → 되묻기.** `AskUserQuestion`이 allowed-tools에 있으면서
  본문 어디에도 사용 지시가 없던 죽은 권한이었다. Stage 1에서 섹션 유형을 묻고,
  "모름·해당 없음" 응답일 때만 섹션 블록을 건너뛴다.
- **과잉교정 브레이크를 본문으로 승격.** 반박된 주장(주어-동사 근접), cohesion ≠
  coherence, 문법 ≠ 소통 기능 세 항목이 `principles.md`에만 있어 실전에서 발동하지
  않았다(그 파일은 출처 질문 시에만 읽힘). Stage 2 "Know the principles' limits"로 이동.
- **C안 정의 명확화** — "①의 의도에서 논증 구조를 다시 유도해 그 구조가 요구하는
  문장을 새로 쓴다. 내용 요소는 저자의 것, 산문은 리뷰어의 것"으로 규정하고,
  3안이 서로 수렴하는 실패 양상을 금지 조항으로 명시.
- **⑤에 권고 신설.** 3안을 나열만 하고 판정 없이 끝나던 것을, ⑤ 첫 줄에서 A/B/C 중
  추천(또는 원문 유지)을 명시하도록 했다. 판정은 3안을 모두 만든 뒤에 내린다.
- **② 말미에 적용 지침 명시 한 줄** — 어떤 체크리스트 블록을 돌렸는지 출력에 드러나게
  해서 누락이 침묵 처리되지 않게 했다 (확인표 전면 도입 대신 경량 방식 채택).
- **문장 추가 허용 조항 신설** — 논리 공백·필수 요소 결손을 메우는 문장 추가를 허용하되,
  데이터·인용·수치 날조 금지와 `[근거 필요]` 마커 두 한계를 절대 조건으로.
- **언어 규칙 유지·명시** — 진단은 한국어, 리라이팅은 원문 언어와 무관하게 항상 영어.
  한국어로 남아야 하는 단락은 `meta-mywriting-korean`으로 안내. 한국어 초고일 때
  문장 스타일 공통은 ④의 영어 출력에 대해 돌린다(원문 표면에 적용하면 오탐).
- Completion criterion에 지침 명시·권고 포함을 추가.

### references — 자체 완결화

- **타 스킬 의존 제거.** `../meta-writing/references/claude_writing_manual/` 참조
  (tier-3 근거 층, DECISIONS.md §D4·§D5, DIAGNOSTIC-MAP.md)를 모두 제거했다.
  meta-rewriting을 단독 배포해도 깨지는 경로가 없다. 근거 층 역할은 `principles.md`가
  단독 수행하고, 미해결 상충 3건(초록 길이·제목 길이·초록의 태)은 파일 내 ⚠ 항목
  + "저널 규정 확인, 없으면 사용자에게 질문" 규칙으로 자체 처리한다.
- **section-checklists.md 정리** — 체크 항목은 전부 유지(무손실). 상단 개정 이력
  인용 블록 4개 제거(CHANGELOG로 이관), 하드코딩 절대경로(`Z:\KKH_Research\...`) 제거,
  빌드 추적 태그(`[회수 2026-08-10 · remined-*]` 계열) 제거. 근거 등급 표기
  (`[1차:]`/`[보조:]`)는 유지. 사용 규칙을 새 SKILL.md의 되묻기 흐름에 맞게 갱신.
- **principles.md** — 원칙 본문 무수정. 상단 개정 주석을 한 줄로 압축.

### 반영하지 않은 것

로컬에서만 존재하던 1.2.0~1.4.0 계열의 확장(6블록·4안 사다리·⑤ 통과 확인표·보고서
본문 유형)은 채택하지 않았다 — 사용자 결정: 3안 유지, 자유 논의 + 문장별 표 유지,
단순한 4단계 흐름 우선.

## 1.1.0 (2026-07-23)

`Writing_Principles_Extraction` 원리 은행(27개 소스, 대표 원리 185개)과 대조한 결과를 반영.
185개 중 이미 커버 65개, 적용 불가 71개, 충돌 7개였고 **실질 순증분은 14~15개**였다.
`section-checklists.md`가 2026-07-16에 바로 그 코퍼스에서 만들어졌으므로 중복이 지배적인 것은
예상된 결과이며, 실제 기여는 "새 지식"이 아니라 **그때 얕게 판 곳(특히 Ecarnot 2015)의 회수**다.

### 버그 수정 — 원리 반영과 무관하게 잘못 동작하던 것

**공통 체크가 섹션 미상 단락에서 실행되지 않았다.** `section-checklists.md`의
`문장 스타일 공통`·`인용·참고문헌 규범` 블록은 파일 안에서 "모든 섹션 적용"으로 명시돼 있는데,
SKILL.md는 **파일 전체**를 섹션 유형 식별 시에만 읽도록 게이팅하고 있었다. 결과적으로 섹션을
알 수 없는 단락에서는 축약형·클리셰·만연체·인용 규범 검사가 아예 돌지 않았다.
읽기 규칙을 2층위(항상 적용 / 섹션 식별 시에만)로 분리했고, 같은 문구가 있던
`section-checklists.md` 헤더의 사용 규칙도 함께 고쳤다.

### 구조적 결함 3개

**1. ④에 "원문 유지"가 선택지로 없었다.**
스킬은 항상 리라이팅 3안을 내놓게 설계돼 있어, 원문이 이미 최선인 경우에도 자기 리라이팅
3개만 제시했다. 자기 산출물 과대평가(self-preference bias)를 경고하는 원리가 이 급소를 짚는다.
④에 "원문이 최선일 수 있다" 항을 넣고 ⑤·Follow-up turns에 `원문 유지`를 선택지로 추가했다.

**2. ②·③에 덜어내는 진단이 없었다.**
결함을 *고치는* 항목만 있고 *빼는* 항목이 없어 잉여 문장은 진단표에 오르지 않았다.
문장 단위 삭제 테스트(빼도 논지·근거가 온전하면 잉여 = 中)를 ②에 추가했다.

**3. 논증 타당성 기준이 0개였다.**
`principles.md`의 8원칙은 전부 *정보가 어떻게 배열되는가*이지 *주장이 타당한가*가 아니다.
원칙 8이 claim-evidence의 형식만 요구하고 그 사이 추론의 건전성은 아무도 묻지 않아,
순환논증·상관을 인과로 도약·표본을 넘는 일반화·자기모순이 무결점 통과했다.
② 논의에 argument soundness 항을 신설했다.

### 그 밖의 SKILL.md 변경

- **③ 심각도 척도에 원리적 근거 부여.** 기존에는 高/中/低 척도와 예시 한 줄뿐이라 무엇이
  高인지 기준이 없었다. "결함이 얼마나 전역적인가"(독자가 잘못 가져가는가 / 느려지는가 /
  다듬기 문제인가)로 정의하고, 수정 순서도 전역 → 국소로 명시했다.
- **② 단락 내부 일관성** 항 추가 — 시제·인칭·용어 혼용. 기존 시제 기준은 섹션 규범 대조용이라
  섹션 유형을 모르면 발동하지 않았다.
- **④ A안 가드레일** — ③에서 "수정 불필요"로 선언한 문장은 A안에서 건드리지 않는다.
- **④ 주장 강도 재보정의 지위 명시.** 가드레일은 hedging 강도·범위·인과 언어의 재보정을
  허용하는데, 이는 다듬기가 아니라 저자가 무엇을 주장하는지를 바꾸는 행위다. 변경 요약에
  이름만 적는 대신 저자 확인을 요구하도록 했다.
- **④ 인용·수치 미검증 책임 이관** — 스킬은 인용·수치를 verbatim 이월할 뿐 실재를 검증하지
  않는다(도구도 없다: `allowed-tools: [Read, Glob]`). 초고가 AI 산출물이면 날조된 인용이
  세 판본에 그대로 복제되므로, 원출처 대조가 저자 몫임을 명시했다.
- **⑤ 반복 결함 요약 한 줄** — 여러 단락에 걸쳐 누적되면 저자 자신의 체크리스트가 된다.

### section-checklists.md 보강

- **Methods 통계 서술 블록 신설** — 기존에는 "소프트웨어·버전 명시" 한 줄뿐이었다. 데이터 제시
  형식, 변수 유형별 검정, 다변량 투입 변수, 표본수 산정 근거, 유의수준, 다중비교 보정,
  하위집단 분석 사전 명시.
- **Methods 윤리 진술 블록 신설** — 항목 자체가 없었다. 윤리위원회 승인, 서면 동의, 등록번호.
  승인번호는 사실 정보이므로 리라이팅에서 생성 금지를 함께 명시.
- **Conclusion 절 신설** — Discussion만 있고 결론 절이 없었다. 새 정보 도입 금지, 종합 여부,
  연구 질문에 대한 답, 기여 진술, hedging 유지.
- Methods: 설계 명시로 시작, 비통상 방법 정당화, 방법-결과 인과 연결, 1차·2차 평가변수.
- Results: 수치 보고 완전성(n·산포·CI), 본문/표/그림 배분 기준, 불리한 결과의 은폐 검출.
- Abstract: 자기완결성(본문 참조 금지), 결과의 1차→2차 순서, 배제 항목 확장(비표준 약어·
  평가적 진술·삽화), 첫 문장 접근성.
- Introduction: 마무리 순서(공백 → 가설 → 목적 → 전략).
- Discussion: 서론 공백 되짚기, 범위 이동(scope creep) 검출, 선행연구 비판의 어조.
- Literature Review: 체계적 고찰의 검색 전략 명시.
- 문장 스타일 공통: 논리 층위 병렬(원칙 8의 문법 병렬에 개념 차원 추가), 미수치화 크기 진술
  (플래그 전용 — 수치를 만들어 넣지 않는다).

### principles.md — 의도적으로 변경하지 않음

추가 후보가 있었으나(원칙 8 Parallelism의 개념 층위 확장) 넣지 않았다. 이 파일의 가치는
"22개 소스, 3중 독립 검증"이라는 검증 방식이 보증하는 출처 신뢰도에 있고, 그 절차를 거치지
않은 항목을 섞으면 보증이 무너진다. 은행에서 온 항목은 전부 `section-checklists.md`에 배치했다.
개정 경위와, 은행에 원칙 1·2·5 대응 항목이 0개라는 대조 결과만 주석으로 남겼다.

### 근거 표기 체계 — 권위 등급제 유지

은행은 모든 소스를 동등 취급하고 **수렴 수**로 신뢰를 표현하지만, `section-checklists.md`는
**권위 등급제**(`[1차: 출판 문헌]`/`[보조: 커뮤니티 공명도]`)를 유지한다. 추가 항목이 전부
출판 문헌 지지를 갖고 있어 무손실 번역되었고, 트윗만 지지하는 항목은 0건이었다.
수렴 수는 본문에 넣지 않는다 — 이 파일 안에서는 수렴이 높을수록 이미 반영돼 있어
**진단 가치와 역상관**이기 때문이다(수렴 최상위 "저널 지침 준수"는 단락 진단 가치 0,
이번 최대 수확인 Methods 통계 블록은 수렴 1).

### 채택하지 않은 것 (충돌 7건)

- **주어 뒤 동사 근접** — `principles.md`의 「반박된 주장 (규칙으로 쓰면 안 됨)」에 이미
  명시적으로 실려 있다. 넣으면 스킬이 자기가 반박한 것을 규칙으로 채택하게 된다.
- **"appears" 삭제** — 원칙 6("가장 흔한 강조 수단은 hedge의 부재")과 정면 충돌. 헤지를
  걷어내면 주장 강도가 무단 상승한다.
- **결론 먼저** — 단락 수준에서 C-C-C 및 stress position과 반대 방향. 정당한 적용 범위는
  초록·논문 전체 배열이며 단락 진단 스킬 밖이다.
- **signposting 권장** — 이 파일이 "메타담화 과잉"을 결함으로 규정하는데 은행은 권장한다.
  필요한 부분은 이미 "학위논문·긴 논문 한정 로드맵 문장"으로 커버돼 있다.
- **초록·서론의 세일즈 피치** — "매력적이어야", "심사자를 낚아야"는 객관 톤 규범 및 hedging
  진단과 충돌한다.
- **의심스러운 사실 확인 요구** — SKILL.md의 `No fact-checking` 스코핑과 충돌하며 도구로도
  불가능하다. 스킬 쪽 스코핑이 옳다고 판단해 경고 문구로 강도를 낮춰 수용했다.

**AI 생성 초고 탐지 조항도 넣지 않았다.** 은행 20개 중 AI 텍스트의 표면적 특징을 기술한
원리가 하나도 없어 근거가 없고, 은행 자신이 감지기의 오탐률과 비원어민 편향을 경고하며
(부록 A 첫 항목), `writing-anti-ai` 스킬이 이미 그 역할을 전담한다.

> ⚠️ 이 버전부터 GitHub `kkhgeo/METASCI` 의 `skills/metasci_writing/meta-rewriting`과
> 내용이 갈라진다. 직전 상태는 `~/.claude/skills_backup/meta-rewriting_pre-principles_2026-07-23/`
> 에 보관되어 있다.

## 1.0.0 (2026-07-16, 소급 지정)

GitHub METASCI 버전(단락 리뷰)으로 교체된 상태. 5블록 출력(의도 요약 / 논리·명확성 논의 /
문장별 진단표 / 리라이팅 3안 / 다음 행동), `principles.md`(2026-07-11 Deep Research),
`section-checklists.md`(2026-07-16).
